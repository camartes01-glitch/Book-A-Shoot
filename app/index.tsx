/**
 * Root Route (/) — Book A Shoot
 *
 * Always renders the marketing Landing page as the initial entry experience
 * for all users (both signed in and guests).
 */

export { default } from "./landing";
